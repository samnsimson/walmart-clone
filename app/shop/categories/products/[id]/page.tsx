import { NextPageProps } from "@/lib/types";
import { NextPage } from "next";

const AllProductsPage: NextPage<NextPageProps> = ({ params, searchParams }) => {
    return <div>AllProductsPage</div>;
};
export default AllProductsPage;
